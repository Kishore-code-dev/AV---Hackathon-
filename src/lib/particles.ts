type Particle = {
    x: number;
    y: number;
    size: number;
    color: string;
    velocity: { x: number; y: number };
};

export default function particles() {
    // Placeholder for robust particle logic if needed
    // Currently using pure CSS in Sparkles.tsx for performance
    return [];
}
